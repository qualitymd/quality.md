{
  "derivedContext": {
    "appliedRatingCriteria": [
      {
        "criterion": "Common failure modes return the contract's error envelope with stable codes callers can branch on.",
        "ratingLevelId": "rating:target"
      },
      {
        "criterion": "Falls short of target with visible gaps or limited evidence, while remaining acceptable.",
        "ratingLevelId": "rating:minimum"
      }
    ],
    "evidenceTargets": [
      {
        "id": "primary-evidence",
        "question": "Do error responses match the contract envelope across common failure modes?",
        "required": true,
        "sourceRefs": [
          "synthetic-source:service-contract",
          "synthetic-source:api/handler-matrix"
        ]
      }
    ]
  },
  "inputs": {
    "ratingLevelIds": [
      "rating:outstanding",
      "rating:target",
      "rating:minimum",
      "rating:unacceptable"
    ],
    "requirementAssessmentBasis": "Do error responses match the contract envelope across common failure modes?"
  },
  "kind": "RequirementEvaluationFrame",
  "schemaVersion": 3,
  "subject": {
    "factorIds": [
      "factor:api::operability"
    ],
    "requirementId": "requirement:api::predictable-error-contracts"
  }
}
