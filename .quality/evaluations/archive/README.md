# Archived evaluation runs

This directory keeps historical dogfood runs that predate the current
`qualitymd` evaluation record contract.

They are retained as historical artifacts only. Current CLI status and release
readiness use top-level run folders under `quality/evaluations/` that conform to
`specs/evaluation-records.md`.
