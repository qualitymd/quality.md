{
  "schemaVersion": 1,
  "areaPath": [
    "schema-structure"
  ],
  "requirement": "Required columns conform to the station schema",
  "factorPaths": [
    [
      "Structural validity"
    ]
  ],
  "ratingResult": {
    "kind": "rated",
    "level": "target",
    "rationale": "All required columns are present, required fields are populated in the reviewed sample, and profiled values match the documented types and ranges. The review did not include independent downstream import evidence, so the result remains at target rather than outstanding."
  },
  "criterionSource": "rating-scale",
  "findings": [
    {
      "locator": "schema/stations.schema.yaml",
      "observation": "The schema declares station_id, name, status, latitude, longitude, and capacity as required columns, and the station table includes each column.",
      "category": "schema-conformance",
      "severity": "info",
      "evidence": [
        {
          "kind": "source",
          "ref": "schema/stations.schema.yaml"
        },
        {
          "kind": "source",
          "ref": "data/stations.csv"
        },
        {
          "kind": "review",
          "ref": "manual schema-to-table comparison"
        }
      ]
    }
  ],
  "recommendations": []
}
