---
title: /quality agent skill
description: The agent skill is the primary way to set up, evaluate, and improve a QUALITY.md.
---

{/* TODO: Refine content. This page is a scaffold — structure first, prose later. */}

The `/quality` skill is the primary interface to QUALITY.md. You work through an
AI assistant or coding agent; the skill manages the `QUALITY.md` file and the
underlying CLI for you.

## Commands

{/* TODO: Expand each command with what it does and when to reach for it. */}

| Command             | What it does                                     |
| ------------------- | ------------------------------------------------ |
| `/quality setup`    | Guided creation of an initial `QUALITY.md`.      |
| `/quality evaluate` | Produce a quality evaluation report.             |
| `/quality improve`  | Refine `QUALITY.md` and compound learning.       |
| `/quality update`   | Update the skill and CLI to the latest versions. |

## How the skill uses the CLI

{/_ TODO: Explain the skill-first model: the skill drives the deterministic CLI,
and users rarely call the CLI directly. Link to the CLI reference. _/}

See the [CLI reference](/cli) for the deterministic commands the skill
builds on.
